apiVersion: ui.k8s.appscode.com/v1alpha1
kind: ResourceDashboard
metadata:
  creationTimestamp: null
  name: kubedb.com-v1alpha2-mongodbs-pod
spec:
  dashboards:
  - title: KubeDB / MongoDB / Pod
    vars:
    - name: namespace
      value: '{{ .metadata.namespace }}'
    - name: db
      value: '{{ .metadata.name }}'
    - name: pod
      type: Target
      value: '{{ .metadata.name }}'
  provider: Grafana
  resource:
    group: ""
