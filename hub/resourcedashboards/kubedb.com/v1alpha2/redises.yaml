apiVersion: meta.k8s.appscode.com/v1alpha1
kind: ResourceDashboard
metadata:
  creationTimestamp: null
  labels:
    k8s.io/group: kubedb.com
    k8s.io/kind: Redis
    k8s.io/resource: redises
    k8s.io/version: v1alpha2
  name: kubedb.com-v1alpha2-redises
spec:
  dashboards:
  - title: KubeDB / Redis / Summary
    vars:
    - name: namespace
      value: .metadata.namespace
    - name: db
      value: .metadata.name
  - if:
      condition: '{{- if or (eq .spec.mode "Cluster") (eq .spec.mode "Sentinel") }}true{{ else }}false{{ end -}}'
    title: KubeDB / Redis / Shards
    vars:
    - name: namespace
      value: .metadata.namespace
    - name: db
      value: .metadata.name
  - title: KubeDB / Redis / Pod
    vars:
    - name: namespace
      value: .metadata.namespace
    - name: db
      value: .metadata.name
  defaultDashboards: true
  provider: Grafana
  resource:
    group: kubedb.com
    kind: Redis
    name: redises
    scope: Namespaced
    version: v1alpha2
